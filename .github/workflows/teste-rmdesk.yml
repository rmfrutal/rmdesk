name: Teste RMDesk

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+-[0-9]+'

jobs:
  ola:
    runs-on: ubuntu-latest
    steps:
      - name: Diz ola
        run: echo "RMDesk - teste de montagem"
